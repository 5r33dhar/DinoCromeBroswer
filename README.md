# DinoCromeBroswer

This script is hybrid approach to automating the Chrome Dino game. Since Chrome's security prevents Selenium from navigating directly to chrome:// URLs, using PyAutoGUI to manually type the address is a solid workaround.
